import HelmetContainer from '../../containers/HelmetContainer';

const NotFoundPage: React.FC = () => {
 return (
  <HelmetContainer title="صفحه ناموجود">
   <p>Not Found Page ...</p>
  </HelmetContainer>
 );
};

export default NotFoundPage;
