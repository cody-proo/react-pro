import HelmetContainer from '../../containers/HelmetContainer';
import { COURSES_TITLE } from '../../core/languages/keys';

const ArchiveCoursesPage: React.FC = () => {
 return (
  <HelmetContainer title={COURSES_TITLE}>
   <p>Archive Courses Page ...</p>
  </HelmetContainer>
 );
};

export default ArchiveCoursesPage;
