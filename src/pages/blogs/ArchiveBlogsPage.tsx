import HelmetContainer from '../../containers/HelmetContainer';
import { BLOGS_TITLE } from '../../core/languages/keys';

const ArchiveBlogsPage: React.FC = () => {
 return (
  <HelmetContainer title={BLOGS_TITLE}>
   <p>Archive Blogs ...</p>
  </HelmetContainer>
 );
};

export default ArchiveBlogsPage;
